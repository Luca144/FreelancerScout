[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers -m "not smoke and not e2e"
markers =
    smoke: Smoke tests that hit the live RSS feed (network required)
    e2e: End-to-end tests that run the full pipeline with mocked HTTP responses
